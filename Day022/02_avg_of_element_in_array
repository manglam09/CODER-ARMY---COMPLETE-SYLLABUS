#include <stdio.h>

int main() {
    int arr[18], i;
    float sum = 0, avg;

    printf("Enter 18 elements:\n");
    for(i = 0; i < 18; i++) {
        scanf("%d", &arr[i]);
        sum += arr[i];
    }

    avg = sum / 18;

    printf("Average of array elements = %.2f\n", avg);

    return 0;
}
